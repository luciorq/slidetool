% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slidetool_region.R
\name{slidetool_region_read}
\alias{slidetool_region_read}
\title{Read a Region from a Slide}
\usage{
slidetool_region_read(path, x, y, level, width, height, output_file = NULL)
}
\arguments{
\item{path}{Character string specifying the path to the slide file.}

\item{x}{Numeric value specifying the x-coordinate in level 0 pixels.}

\item{y}{Numeric value specifying the y-coordinate in level 0 pixels.}

\item{level}{Integer specifying the slide level to read from (0 is the highest resolution).}

\item{width}{Numeric value specifying the width of the region in pixels at the specified level.}

\item{height}{Numeric value specifying the height of the region in pixels at the specified level.}

\item{output_file}{Character string specifying the path to the output PNG file. If \code{NULL}, the image will be written to a temporary file output.}
}
\value{
Invisibly returns the path to the output file.
}
\description{
Extracts a specific region from a slide and saves it as a PNG image.
}
\details{
Coordinates \code{x} and \code{y} are specified in pixels at level 0 (highest resolution).
The dimensions \code{width} and \code{height} are specified at the requested \code{level}.
This function is useful for extracting regions of interest from large slide images.
}
\examples{
\dontrun{
# Read a region and save to a file
region_path <- slidetool_region_read(
  path = fs::path_package("slidetool", "extdata", "CMU-1-Small-Region.svs"),
  x = 1000,
  y = 1000,
  level = 0,
  width = 500,
  height = 500
)
}

}
